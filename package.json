{
  "name": "xinyunopen-bot",
  "private": false,
  "version": "0.1.24",
  "type": "module",
  "main": "electron/main.mjs",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "vite",
    "dev:server": "node --experimental-strip-types server/index.ts",
    "dev:desktop": "electron .",
    "build": "tsc -b && tsc -p tsconfig.server.json && vite build",
    "typecheck": "tsc -b && tsc -p tsconfig.server.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:electron": "node --check electron/main.mjs && node --check electron/preload.cjs && node --check electron/capabilities.cjs && node --check electron/build-speech-helper.mjs && node --check electron/cua.mjs && node --check electron/speech.mjs && node --check electron/updater.mjs && node --check scripts/notarize.cjs && node --check scripts/prepare-cua.mjs && node --check scripts/smoke-mac-package.mjs && node --check scripts/verify-mac-artifacts.mjs",
    "preview": "vite preview",
    "build:server": "tsc -p tsconfig.server.build.json",
    "build:speech": "node electron/build-speech-helper.mjs",
    "build:cua": "node scripts/prepare-cua.mjs",
    "build:updater": "node scripts/bundle-updater.mjs",
    "package:prepare": "pnpm build && pnpm build:server && pnpm build:updater",
    "package:mac": "pnpm package:mac:arm64",
    "package:mac:arm64": "pnpm package:prepare && pnpm build:speech && pnpm build:cua && electron-builder --mac dmg zip --arm64 --publish never",
    "package:mac:x64": "pnpm package:prepare && pnpm build:speech && pnpm build:cua && electron-builder --mac dmg zip --x64 --publish never",
    "package:win": "pnpm package:prepare && electron-builder --win --publish never",
    "package": "pnpm package:mac"
  },
  "dependencies": {
    "@trycua/cua-driver": "0.19.3",
    "clsx": "^2.1.1",
    "lucide-react": "^0.539.0",
    "posthog-js": "^1.415.6",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1",
    "shiki": "^4.4.3",
    "tailwind-merge": "^3.3.1"
  },
  "optionalDependencies": {
    "@trycua/cua-driver-darwin-arm64": "0.19.3",
    "@trycua/cua-driver-darwin-x64": "0.19.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.11",
    "@types/node": "^26.2.0",
    "@types/react": "^19.1.9",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^4.7.0",
    "electron": "^43.4.0",
    "electron-builder": "^26.15.3",
    "electron-updater": "^6.8.9",
    "esbuild": "^0.28.2",
    "tailwindcss": "^4.1.11",
    "typescript": "^5.8.3",
    "vite": "^7.1.0",
    "vitest": "^4.1.10"
  },
  "description": "XinyunOpen Bot — 面向中国用户的开源 Grok Bot 替代方案，默认简体中文。",
  "author": "Dongbeibei",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dongpen-max/XinyunOpenBot.git"
  },
  "homepage": "https://github.com/dongpen-max/XinyunOpenBot",
  "bugs": {
    "url": "https://github.com/dongpen-max/XinyunOpenBot/issues"
  }
}
