# XinyunOpen Bot 使用指南

## 简介

XinyunOpen Bot 是一个多 AI 聊天客户端，支持 Claude、OpenAI/Codex 和 xAI/Grok。每个机器人可以拥有自己的计算机环境，执行真实的任务。

## 一键启动

双击桌面上的 **XinyunOpen Bot** 快捷方式即可启动全部服务。

快捷方式会依次启动：
1. **后端服务器** (端口 8799) - 后台运行
2. **前端开发服务器** (端口 5199) - 后台运行  
3. **Electron 桌面应用** - 主窗口

## 首次使用

### 1. 配置 API 中转站

编辑 `<项目目录>\.env.local`，填写您的中转站信息：

```bash
# Claude 中转站
ANTHROPIC_API_KEY=your_claude_api_key
ANTHROPIC_BASE_URL=https://your-claude-proxy.com/v1

# OpenAI 中转站（可选）
OPENAI_API_KEY=your_openai_api_key
OPENAI_BASE_URL=https://your-openai-proxy.com/v1

# xAI/Grok 中转站（可选）
XAI_API_KEY=your_xai_api_key
XAI_BASE_URL=https://your-xai-proxy.com/v1
```

详细配置说明请查看 [API配置说明.md](./API配置说明.md)

### 2. 启动应用

双击桌面快捷方式或运行：

```bash
<项目目录>\start-all.bat
```

### 3. 完成引导

首次启动会进入欢迎界面：

1. **个人信息**（可跳过）
   - 填写姓名和邮箱以接收更新通知
   
2. **AI 引擎检测**
   - 自动检测已配置的 AI 服务
   - ✅ 绿色勾选表示可用
   - ⚠️ 橙色警告表示需要配置
   
3. **权限设置**（可选，仅 macOS）
   - 麦克风权限：用于语音输入
   - 屏幕录制：用于机器人查看屏幕

## 核心功能

### 创建机器人

1. 点击左侧边栏底部的 **➕ 新建机器人**
2. 选择 AI 引擎（Claude / Codex / Grok）
3. 开始对话

### 切换模型

点击右上角的模型选择器，可以切换：

- **Claude**: Opus 5 / Sonnet 5 / Haiku 4.5 / Fable 5
- **Codex**: GPT-5.6 Sol / Terra / GPT-5.4
- **Grok**: Grok 4 / Grok 4 Fast / Grok 3 Mini

### 机器人的计算机

每个机器人可以拥有自己的隔离计算机环境：

1. 点击右上角的 **🖥️ 电脑图标**
2. 选择计算机类型：
   - **云端计算机**: 隔离的 Linux 环境
   - **本地 Mac**: 控制您的 Mac（仅 macOS）

机器人可以在计算机上：
- 查看屏幕截图
- 执行命令
- 操作鼠标和键盘

### 对话功能

- **发送消息**: 在底部输入框输入后按 Enter
- **停止回复**: 点击右上角的 **停止** 按钮
- **语音输入**: 点击输入框右侧的麦克风图标（需授权）

### 管理机器人

右键点击左侧边栏的机器人卡片，可以：

- **重命名机器人**
- **置顶/取消置顶**
- **复制机器人**（克隆对话历史）
- **复制会话 ID**（用于调试）
- **隐藏机器人**（归档）
- **删除机器人**

### 插件系统

点击左侧边栏底部的 **🧩 插件** 图标：

- 连接外部应用（通过 Composio）
- 查看已安装的插件
- 浏览插件市场

## 浏览器访问

除了桌面端，也可以直接在浏览器中使用：

```
http://127.0.0.1:5199
```

桌面端特有功能：
- 本地屏幕捕获
- 语音输入（仅 macOS）
- 系统级 CUA 集成

## 停止服务

### 方法 1: 任务管理器

1. 打开任务管理器 (`Ctrl+Shift+Esc`)
2. 找到所有 `node.exe` 进程
3. 结束任务

### 方法 2: 命令行

```powershell
# 停止所有 node 进程
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force

# 停止所有 electron 进程
Get-Process electron -ErrorAction SilentlyContinue | Stop-Process -Force
```

## 数据存储

用户数据保存在：

```
C:\Users\你的用户名\.openmausbot\
├── config.json           # 应用配置
├── events\               # 对话历史
└── native\               # 原始协议日志
```

## 故障排查

### 1. 桌面应用窗口白屏

**原因**: 前端或后端服务未启动

**解决**:
1. 检查是否有 `node.exe` 进程在运行
2. 关闭所有窗口
3. 重新运行 `start-all.bat`

### 2. 显示"未登录"错误

**原因**: API 配置未生效

**解决**:
1. 确认 `.env.local` 文件存在且格式正确
2. 检查是否同时配置了 `API_KEY` 和 `BASE_URL`
3. 重启所有服务

### 3. Electron 下载失败

**原因**: 网络问题

**解决**: 启动脚本已自动使用国内镜像（npmmirror），通常可以成功下载

### 4. 端口被占用

**错误**: `EADDRINUSE: address already in use`

**解决**:
```powershell
# 查找占用 8799 端口的进程
netstat -ano | findstr "8799"

# 结束该进程（替换 <PID> 为实际进程号）
taskkill /PID <PID> /F
```

### 5. 对话无响应

**可能原因**:
- API 密钥错误
- 中转站不可访问
- 网络超时

**解决**:
1. 检查 `.env.local` 配置
2. 在浏览器中测试中转站 URL 是否可访问
3. 查看控制台错误信息

## 手动启动（高级）

如果一键启动失败，可以手动分步启动：

### 1. 启动后端服务器

```bash
cd XinyunOpenBot
pnpm dev:server
```

### 2. 启动前端（新终端）

```bash
cd XinyunOpenBot
pnpm dev
```

### 3. 启动 Electron（新终端）

```bash
cd XinyunOpenBot
pnpm dev:desktop
```

## 更新应用

```bash
cd XinyunOpenBot

# 拉取最新代码
git pull

# 重新安装依赖
pnpm install

# 重启服务
# 双击桌面快捷方式即可
```

## 技术栈

- **前端**: React + TypeScript + Vite
- **后端**: Node.js + TypeScript
- **桌面端**: Electron
- **AI 驱动**: Claude CLI / Codex CLI / Grok CLI
- **协议**: JSON-RPC 2.0 / ACP / stream-json

## 获取帮助

- **项目主页**: https://github.com/milind-soni/XinyunOpen Bot
- **问题反馈**: 提交 GitHub Issue

## 许可证

本项目为开源项目，请查看 LICENSE 文件了解详情。
